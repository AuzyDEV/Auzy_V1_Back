
class sharedPost {
    constructor(id, postId, postContenu, postPhoto, adminName, adminPhoto, currentUserId, currentUserName, currentUserphoto, idSharedUser,dateShare) {
      this.id = id; // represents the id generated by the firestore
      // post s infos
      this.postId= postId;
      this.postContenu = postContenu;
      this.postPhoto= postPhoto;
      // admin s infos who creates the post => admin infos
      this.adminName = adminName;
      this.adminPhoto = adminPhoto;
      // current user 's infos : who will share the post
      this.currentUserId = currentUserId;
      this.currentUserName = currentUserName;
      this.currentUserphoto= currentUserphoto;
      // info about sharing post
      this.idSharedUser = idSharedUser; // id of user that i shared with
      this.dateShare = dateShare; 
    }
  }
   
  module.exports = sharedPost;